import attr
import logging

@attr.s(auto_attribs=True)
class DefaultSetupVisual:
   """
   canvas
   width: int
   height: int

   min_freq
   max_freq

   stack - keep all defaults here instead?

   annimation_active : bool
   annimation refresh rate (spped?)
   placement
   size
   annimation_duration
   annimation_type

   base Color class - needed?
   hex (default)
   transparency
   stack position
   animate
   """